#!/usr/bin/env bash
# AppImage entry point. Exports APPDIR and hands off to the orchestrator.
set -u

SELF="$(readlink -f "$0" 2>/dev/null || echo "$0")"
export APPDIR="${APPDIR:-${SELF%/*}}"
export PATH="$APPDIR/usr/bin:$PATH"

exec bash "$APPDIR/usr/bin/mc-education" "$@"